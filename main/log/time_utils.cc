// #include "time_utils.h" // El nuevo nombre de tu cabecera
// #include <ctime>        // Cabecera estándar de C++ para tiempo
// #include <sys/time.h>   // Cabecera de sistema para timeval

// void getTimeString(char *buffer, size_t len)
// {
// time_t now;
// struct tm timeinfo;

// time(&now);
// localtime_r(&now, &timeinfo);
// strftime(buffer, len, "%H:%M:%S", &timeinfo);
// }
